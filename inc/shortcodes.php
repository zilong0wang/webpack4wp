<?php
/**
 * Shortcodes
 */

/* function sample_shortcode($atts) {
    $atts = shortcode_atts(array(
        'post-type' => 'post',
        'posts-per-page'   => -1,
        'orderby'   => 'date',
        'order'     => 'asc',
        'column'    => 4,
        'class'     => '',
        'title'     => ''
    ), $atts);
    ob_start();
    include(locate_template('template-parts/path-to-shortcode-templ.php'));
    $content = ob_get_clean();
    return $content;
}
add_shortcode( 'sample', 'sample_shortcode' ); */
